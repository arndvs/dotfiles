# secrets/.env.bridge.example — Webhook secret only
# Copy to .env.bridge and set the value.
# Loaded ONLY by the webhook systemd unit (not the worker).

# Required: GitHub webhook secret for HMAC signature verification
WEBHOOK_SECRET=

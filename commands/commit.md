Load the atomic-commits skill from ~/dotfiles/skills/atomic-commits/SKILL.md. Survey the diff, group into logical units, and commit with conventional messages. Use Commit mode by default; use Ship mode if I say "ship" or "push".

$ARGUMENTS
